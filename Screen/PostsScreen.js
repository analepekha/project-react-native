// import { getHeaderTitle } from "@react-navigation/elements";
import { Text } from "react-native";

export default function PostsScreen({ navigation, options, route }) {
  //   const title = getHeaderTitle(options, route.name);

  return (
    <>
      <Text>Hello</Text>
    </>
  );
}
